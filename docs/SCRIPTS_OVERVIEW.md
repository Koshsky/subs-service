# Обзор скриптов проекта

## Краткое описание

Проект содержит набор специализированных скриптов для различных операций разработки, тестирования и развертывания. Скрипты организованы по принципу единой ответственности и могут использоваться как независимо, так и в комбинации.

## Структура скриптов

```
scripts/
├── 🏗️  Основные скрипты
│   ├── build.sh               # Главный оркестратор (243 строки)
│   ├── setup.sh               # Настройка окружения (317 строк)
│   └── docker.sh              # Операции с Docker (328 строк)
│
├── 🧪 Тестирование
│   ├── health-check.sh        # Проверка здоровья сервисов (177 строк)
│   ├── test-api.sh            # Тестирование API (203 строки)
│   └── test-notification.sh   # Тестирование уведомлений (66 строк)
│
├── ⚙️  Настройка окружения
│   ├── create-env.sh          # Создание .env файла (91 строка)
│   ├── validate-env.sh        # Валидация переменных (358 строк)
│   ├── generate-certs.sh      # TLS сертификаты (61 строка)
│   ├── generate-proto.sh      # Protobuf код (65 строк)
│   └── update-dependencies.sh # Go зависимости (47 строк)
```

## Детальное описание

### 🏗️ Основные скрипты

#### `build.sh` - Главный оркестратор
**Назначение**: Координация других скриптов, предоставление простого интерфейса
**Размер**: 243 строки
**Команды**: `setup`, `build`, `full`, `clean`, `reset`
```bash
./scripts/build.sh full              # Полная сборка
./scripts/build.sh build --no-cache  # Сборка без кэша
./scripts/build.sh reset             # Сброс и пересборка
```

#### `setup.sh` - Настройка окружения
**Назначение**: Подготовка окружения для разработки
**Размер**: 317 строк
**Команды**: `env`, `cert`, `proto`, `deps`, `validate`, `check`, `all`
```bash
./scripts/setup.sh all              # Все операции настройки
./scripts/setup.sh env --force      # Принудительное создание .env
./scripts/setup.sh check            # Проверка требований
```

#### `docker.sh` - Операции с Docker
**Назначение**: Управление Docker контейнерами и образами
**Размер**: 328 строк
**Команды**: `build`, `verify`, `cleanup`, `clear-volumes`, `start-base`, `stop`, `logs`, `status`
```bash
./scripts/docker.sh build --no-cache  # Сборка без кэша
./scripts/docker.sh logs auth-service # Логи auth-service
./scripts/docker.sh status            # Статус контейнеров
```

### 🧪 Скрипты тестирования

#### `health-check.sh` - Проверка здоровья сервисов
**Назначение**: Мониторинг состояния всех сервисов
**Размер**: 177 строк
**Функции**:
- Проверка доступности сервисов
- Проверка баз данных
- Проверка RabbitMQ
- Детальная диагностика

#### `test-api.sh` - Тестирование API
**Назначение**: Автоматизированное тестирование API endpoints
**Размер**: 203 строки
**Функции**:
- Тестирование auth-service
- Тестирование core-service
- Проверка JWT токенов
- Тестирование подписок

#### `test-notification.sh` - Тестирование уведомлений
**Назначение**: Проверка интеграции notification service
**Размер**: 66 строк
**Функции**:
- Проверка RabbitMQ
- Проверка notification service
- Проверка auth service
- Инструкции по тестированию

### ⚙️ Скрипты настройки окружения

#### `create-env.sh` - Создание .env файла
**Назначение**: Генерация файла с переменными окружения
**Размер**: 91 строка
**Функции**:
- Создание .env с дефолтными значениями
- Валидация созданного файла
- Безопасность (предупреждения)

#### `validate-env.sh` - Валидация переменных окружения
**Назначение**: Проверка корректности переменных окружения
**Размер**: 358 строк
**Функции**:
- Проверка обязательных переменных
- Валидация форматов (порты, URL, пароли)
- Проверка JWT секретов
- Детальные отчеты об ошибках

#### `generate-certs.sh` - TLS сертификаты
**Назначение**: Генерация самоподписанных сертификатов
**Размер**: 61 строка
**Функции**:
- Создание приватного ключа
- Генерация сертификата с SAN
- Настройка прав доступа
- Поддержка localhost и сервисов

#### `generate-proto.sh` - Protobuf код
**Назначение**: Генерация Go кода из .proto файлов
**Размер**: 65 строк
**Функции**:
- Генерация gRPC кода
- Обновление go.mod
- Проверка зависимостей

#### `update-dependencies.sh` - Go зависимости
**Назначение**: Обновление Go модулей
**Размер**: 47 строк
**Функции**:
- Обновление go.mod
- Обновление go.sum
- Проверка совместимости

## Анализ необходимости build.sh

### Аргументы ЗА сохранение build.sh:

1. **Удобство для новых разработчиков**
   - Один скрипт для полной сборки
   - Простой интерфейс без знания деталей
   - Автоматическое определение необходимых операций

2. **Стандартизация процессов**
   - Единая точка входа для CI/CD
   - Консистентные команды для команды
   - Документированные workflow

3. **Оркестрация сложных операций**
   - Координация нескольких скриптов
   - Обработка зависимостей между операциями
   - Единообразная обработка ошибок

### Аргументы ПРОТИВ build.sh:

1. **Дублирование функциональности**
   - Все команды уже есть в специализированных скриптах
   - Дополнительный слой абстракции
   - Больше кода для поддержки

2. **Сложность отладки**
   - Дополнительный уровень вложенности
   - Сложнее найти источник проблемы
   - Меньше прозрачности

3. **Нарушение принципа единственной ответственности**
   - Скрипт выполняет роль оркестратора
   - Смешивание логики координации и выполнения

## Рекомендация

**Сохранить `build.sh`** как главный оркестратор, но с упрощенной ролью:

### Предлагаемая роль build.sh:
- **Только оркестрация** - не дублировать логику
- **Простой интерфейс** - для быстрого старта
- **CI/CD интеграция** - стандартные команды
- **Документация** - ссылки на специализированные скрипты

### Упрощенная структура команд:
```bash
./scripts/build.sh full              # setup.sh all + docker.sh build
./scripts/build.sh setup             # setup.sh all
./scripts/build.sh build             # docker.sh build
./scripts/build.sh clean             # docker.sh cleanup
./scripts/build.sh reset             # docker.sh clear-volumes + full
```

## Лучшие практики использования

### Для разработчиков:
```bash
# Первый запуск
./scripts/build.sh full

# Повседневная работа
./scripts/docker.sh build
./scripts/docker.sh logs auth-service
./scripts/health-check.sh
```

### Для CI/CD:
```bash
# Стандартные команды
./scripts/build.sh full
./scripts/test-api.sh
./scripts/health-check.sh
```

### Для отладки:
```bash
# Специализированные скрипты
./scripts/setup.sh check
./scripts/docker.sh status
./scripts/validate-env.sh
```

## Заключение

Текущая архитектура скриптов хорошо сбалансирована:
- **Специализированные скрипты** для конкретных задач
- **Главный оркестратор** для удобства и стандартизации
- **Четкое разделение ответственности**
- **Гибкость использования**

Рекомендуется сохранить `build.sh` как тонкий слой оркестрации, не дублирующий функциональность специализированных скриптов.
